import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  circle: {
    width: 100,
    height: 100,
    borderRadius: 999,
    backgroundColor: 'blue',
    position: 'absolute',
  },
});
